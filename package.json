{
  "name": "gocd-lens",
  "version": "1.1.2",
  "private": true,
  "description": "A Chrome extension that reads GoCD pipelines from the API and draws its own dashboard — a modern, fast alternate UI for GoCD.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "test:watch": "node --test --watch \"tests/*.test.mjs\"",
    "icons": "python3 tools/make-icons.py",
    "sounds": "python3 tools/make-sounds.py",
    "screenshots": "node tools/make-screenshots.mjs",
    "store": "node tools/make-screenshots.mjs --store",
    "site": "node tools/make-screenshots.mjs --site",
    "hooks": "git config core.hooksPath .githooks",
    "package": "rm -f gocd-lens.zip && zip -r gocd-lens.zip manifest.json icons sounds src -x '*.DS_Store'"
  },
  "engines": {
    "node": ">=20"
  }
}
